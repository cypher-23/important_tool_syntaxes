wpscan 

full scan: **wpscan --url urllink**

```linux
wpscan --url urllink
```


enumerate the site &lt;full enumeration&gt; : **wpscan -e --url urllink**

```linux
wpscan -e --url urllink
```

enumerate users: **wpscan -e U --url urllink**

```linux
wpscan -e U --url urllink
```

enumerate plugins:  **wpscan -e ap --url urllink**

```linux
wpscan -e ap --url urllink
```


enumerate themes : **wpscan -e at --url urllink**

```linux
wpscan -e at ---url urllink
```

enumerate vulnerable plugins: **wpscan -e vp --url urllink**

```Linux
wpscan -e vp --url urllink
```


brute-forcing: **wpscan -t 50 -U username -P /link/to/dictionary**

```Linux
wpscan -t 50 -U username -P /link/to/dictionary
```

**wpscan -t 50 -U username -P /link/to/dictionary**
```Linux
wpscan -t 50 -U username -P /link/to/dictionary
```
